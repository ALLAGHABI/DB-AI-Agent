# دليل التشغيل - DB-AI-Agent
## وكيل قاعدة البيانات الذكي

---

## 📋 **نظرة عامة**

DB-AI-Agent هو تطبيق ذكي يتيح لك التفاعل مع قواعد البيانات باستخدام اللغة الطبيعية العربية والإنجليزية. يحول طلباتك إلى استعلامات SQL تلقائياً باستخدام الذكاء الاصطناعي.

### ✨ **المميزات الرئيسية:**
- 🤖 تحويل اللغة الطبيعية إلى SQL باستخدام OpenRouter AI
- 🗄️ دعم SQLite, MySQL, PostgreSQL
- 🔒 تشفير آمن للإعدادات وكلمات المرور
- 🌐 واجهة ويب عربية سهلة الاستخدام
- 💾 حفظ الإعدادات تلقائياً في المتصفح
- 🎯 اختيار من 7 نماذج ذكاء اصطناعي مختلفة

---

## 🚀 **التثبيت والإعداد**

### 1. **متطلبات النظام:**
```bash
Python 3.8+
pip (مدير حزم Python)
```

### 2. **تثبيت المتطلبات:**
```bash
cd /Users/apple/Desktop/DB-AI-Agent
pip install -r requirements.txt
```

### 3. **تشغيل التطبيق:**
```bash
python main.py
```

### 4. **فتح التطبيق:**
افتح المتصفح وانتقل إلى: `http://localhost:8080`

---

## 🔑 **الحصول على مفتاح OpenRouter API**

### الخطوات:
1. انتقل إلى [OpenRouter.ai](https://openrouter.ai)
2. أنشئ حساب جديد أو سجل الدخول
3. اذهب إلى "API Keys" في لوحة التحكم
4. أنشئ مفتاح API جديد
5. انسخ المفتاح (يبدأ بـ `sk-or-`)

### 💰 **الأسعار:**
- معظم النماذج تكلف $0.001-$0.01 لكل 1000 رمز
- يمكنك البدء برصيد مجاني $1-5

---

## 📖 **دليل الاستخدام**

### 1. **الإعداد الأولي:**

#### أ) إعداد OpenRouter API:
1. أدخل مفتاح API في حقل "مفتاح OpenRouter API"
2. اختر نموذج الذكاء الاصطناعي المفضل:
   - **Claude 3.5 Sonnet** (الأفضل للعربية)
   - **GPT-4o** (سريع ودقيق)
   - **GPT-4o Mini** (اقتصادي)
   - **Gemini Pro 1.5** (جيد للنصوص الطويلة)
   - **Llama 3.1 405B** (مفتوح المصدر)
   - **Llama 3.1 70B** (متوازن)
   - **Mistral Large** (سريع)

3. اضغط "اختبار الاتصال" للتأكد

#### ب) إعداد قاعدة البيانات:

**للـ SQLite:**
1. اختر "SQLite" من القائمة
2. اضغط "تصفح" لاختيار ملف .db
3. أو استخدم "قاعدة البيانات التجريبية"
4. اضغط "اتصال"

**للـ MySQL/PostgreSQL:**
1. اختر نوع قاعدة البيانات
2. أدخل معلومات الاتصال:
   - الخادم (مثال: localhost)
   - المنفذ (3306 للـ MySQL، 5432 للـ PostgreSQL)
   - اسم قاعدة البيانات
   - اسم المستخدم
   - كلمة المرور
3. اضغط "اتصال"

### 2. **استخدام التطبيق:**

#### أمثلة على الطلبات العربية:
```
اعرض جميع العملاء
كم عدد العملاء في الرياض؟
أضف عميل جديد اسمه أحمد من جدة
احذف العميل رقم 5
عدّل اسم العميل رقم 3 إلى محمد
اعرض أكثر 5 منتجات مبيعاً
ما هو متوسط أسعار المنتجات؟
```

#### أمثلة على الطلبات الإنجليزية:
```
Show all customers
How many orders were placed today?
Add a new product with name "Laptop" and price 1500
Delete customer with ID 10
Update product price to 200 where name is "Mouse"
Show top 5 selling products
What is the average order value?
```

### 3. **فهم النتائج:**

#### عند نجاح الطلب:
- ✅ **استعلام SQL:** يظهر الكود المُولد
- 📊 **النتائج:** تظهر في جدول منظم
- ℹ️ **معلومات إضافية:** عدد الصفوف المتأثرة

#### عند وجود خطأ:
- ❌ **رسالة الخطأ:** توضح سبب المشكلة
- 🔧 **اقتراحات الحل:** نصائح لإصلاح المشكلة

---

## 🛠️ **استكشاف الأخطاء وإصلاحها**

### المشاكل الشائعة:

#### 1. **"مفتاح API غير صحيح"**
**الحل:**
- تأكد من نسخ المفتاح كاملاً
- تحقق من صلاحية المفتاح في OpenRouter
- تأكد من وجود رصيد في الحساب

#### 2. **"فشل الاتصال بقاعدة البيانات"**
**الحل:**
- تحقق من مسار ملف SQLite
- تأكد من صحة معلومات الاتصال للـ MySQL/PostgreSQL
- تحقق من تشغيل خادم قاعدة البيانات

#### 3. **"لم يتم الحصول على رد من الذكاء الاصطناعي"**
**الحل:**
- جرب نموذج ذكاء اصطناعي مختلف
- تأكد من وضوح الطلب
- تحقق من الاتصال بالإنترنت

#### 4. **"خطأ في تنفيذ الاستعلام"**
**الحل:**
- تحقق من وجود الجداول والأعمدة المطلوبة
- تأكد من صحة أسماء الجداول في الطلب
- اضغط "تحديث المخطط" لإعادة فحص قاعدة البيانات

---

## 🔒 **الأمان والخصوصية**

### حماية البيانات:
- 🔐 **تشفير محلي:** كلمات المرور مشفرة محلياً
- 🚫 **عدم حفظ كلمات المرور:** في localStorage للأمان
- 🔑 **مفاتيح API آمنة:** محفوظة بتشفير محلي
- 🌐 **اتصال آمن:** جميع الطلبات عبر HTTPS

### نصائح الأمان:
- لا تشارك مفتاح API مع الآخرين
- استخدم كلمات مرور قوية لقواعد البيانات
- راجع الاستعلامات قبل تنفيذها على بيانات مهمة
- احتفظ بنسخ احتياطية من قواعد البيانات

---

## 📁 **هيكل المشروع**

```
DB-AI-Agent/
├── main.py                 # ملف التشغيل الرئيسي
├── requirements.txt        # متطلبات Python
├── app/                   # مجلد التطبيق الرئيسي
│   ├── __init__.py
│   ├── web_ui.py          # واجهة الويب
│   ├── ai_agent.py        # وكيل الذكاء الاصطناعي
│   ├── database_handler.py # مدير قاعدة البيانات
│   └── settings_manager.py # مدير الإعدادات
├── templates/             # قوالب HTML
│   └── index.html
├── data/                  # ملفات البيانات
│   ├── sample_store.db    # قاعدة بيانات تجريبية
│   └── schema_cache.json  # ذاكرة التخزين المؤقت للمخطط
└── utils/                 # أدوات مساعدة
    └── voice_recognition.py
```

---

## 🎯 **نصائح للاستخدام الأمثل**

### للحصول على أفضل النتائج:

#### 1. **صياغة الطلبات:**
- كن واضحاً ومحدداً
- استخدم أسماء الجداول والأعمدة الصحيحة
- اذكر الشروط بوضوح

#### 2. **اختيار النموذج:**
- **Claude 3.5 Sonnet:** الأفضل للنصوص العربية المعقدة
- **GPT-4o Mini:** للاستعلامات البسيطة والسريعة
- **GPT-4o:** للاستعلامات المعقدة والدقيقة

#### 3. **إدارة التكلفة:**
- ابدأ بالنماذج الاقتصادية
- راقب استهلاك الرصيد في OpenRouter
- استخدم طلبات واضحة لتقليل المحاولات

---

## 🆘 **الدعم والمساعدة**

### في حالة وجود مشاكل:

1. **تحقق من ملف السجل:** راجع رسائل الخطأ في terminal
2. **أعد تشغيل التطبيق:** `Ctrl+C` ثم `python main.py`
3. **امسح الإعدادات:** اضغط "مسح الإعدادات المحفوظة"
4. **تحديث المتطلبات:** `pip install -r requirements.txt --upgrade`

### معلومات تقنية:
- **المنفذ:** 8080
- **البروتوكول:** HTTP
- **المتصفحات المدعومة:** Chrome, Firefox, Safari, Edge
- **أنظمة التشغيل:** Windows, macOS, Linux

---

## 📝 **سجل التحديثات**

### الإصدار الحالي:
- ✅ إصلاح مشكلة تشفير UTF-8 مع النصوص العربية
- ✅ دعم 7 نماذج ذكاء اصطناعي مختلفة
- ✅ حفظ الإعدادات تلقائياً في المتصفح
- ✅ واجهة مستخدم محسّنة
- ✅ معالجة أخطاء شاملة
- ✅ دعم قواعد بيانات متعددة

---

## 🎉 **ابدأ الآن!**

1. شغّل التطبيق: `python main.py`
2. افتح المتصفح: `http://localhost:8080`
3. أدخل مفتاح OpenRouter API
4. اتصل بقاعدة البيانات
5. ابدأ في طرح أسئلتك!

**مثال سريع:**
```
"اعرض جميع العملاء من الرياض"
```

---

*تم إنشاء هذا الدليل لمساعدتك في الاستفادة القصوى من DB-AI-Agent. استمتع بتجربة التفاعل مع قواعد البيانات بالذكاء الاصطناعي! 🚀*
